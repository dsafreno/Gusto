<div data-role="header" id="page_header">
  <%= link_to "Sign out", "/sessions/destroy", method: :delete %>
  <h1>My Locations</h1>
  <a id="userloc" href="#">CurrLoc</a>
</div>

<div data-role="content">
  <%= render "search_form" %>
  <%= render "loc_form" %>
  <h1 style="text-align: center;">History</h1>
  <% if @pins.empty? %>
    <% if session[:user_id] %>
      <p>You don't have a history! Make your first search above or press the button in the top right to go to your current location.</p>
    <% else %>
      <p> You are not logged in, so we unfortunately cannot store your history. Please make an account or sign in!</p>
    <% end %>
  <% else %>
    <ul data-role="listview" data-inset="true">
      <% @pins.each do |pin| %>
        <li><%= link_to pin.name, pin %></li>
      <% end %>
    </ul>
  <% end %>
</div>
<br/>
